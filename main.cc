#include "Board.h"
#include <iostream>
using namespace std;

int main(){
    Board myBoard;
    cout << myBoard;

    string command;
    std::cin >> command;


    // work on what happens when the user enters setup mode
}